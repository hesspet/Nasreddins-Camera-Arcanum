<MudThemeProvider Theme="@_theme">
    <MudDialogProvider />
    <MudSnackbarProvider MaxDisplayedSnackbars="2" PreventDuplicates="true" />

    <Router AppAssembly="@typeof(App).Assembly">
        <Found Context="routeData">
            <RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />
            <FocusOnNavigate RouteData="@routeData" Selector="main" />
        </Found>
        <NotFound>
            <PageTitle>Not found</PageTitle>
            <LayoutView Layout="@typeof(MainLayout)">
                <MudPaper Class="pa-4" Elevation="1">
                    <MudText Typo="Typo.h6" Color="Color.Error">Sorry, there's nothing at this address.</MudText>
                    <MudText Typo="Typo.body2">Use the menu to return to the Home page.</MudText>
                </MudPaper>
            </LayoutView>
        </NotFound>
    </Router>
</MudThemeProvider>

@code {
    private readonly MudTheme _theme = new()
    {
        PaletteLight = new PaletteLight
        {
            Primary = Colors.Blue.Darken3,
            Secondary = Colors.Cyan.Darken3,
            Background = Colors.Gray.Lighten5,
            AppbarBackground = Colors.Blue.Darken3,
        }
    };
}
